<h1>Create a new article</h1>
    <% if @article.errors.any? %>
    <h2> the following errrors are prevented article from being saved </h2>
       <% end %>
        <%= form_with scope: :article, url: articles_path, local: true do |f| %>
            <p>
                 <%= f.label :title %>
                 <%= f.text_field :title %>
            </p>

            <p>
                 <%= f.label :description %>
                 <%= f.text_area :description %>
            </p>
        
            <p>
                 <%= f.submit %>
            </p>
    <% end %>